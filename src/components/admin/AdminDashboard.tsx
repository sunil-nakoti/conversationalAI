import React from 'react';

const AdminDashboard: React.FC = () => {
  return (
    <div>
      <h2>Admin Dashboard</h2>
      {/* Future: Add user management, analytics, etc. */}
    </div>
  );
};

export default AdminDashboard;
